# ---------------------------------------------------------------
#  TEMPLATE - copy to .env then fill real values (NEVER commit .env)
#  Generate strong secrets: powershell -File .\generate-secrets.ps1
# ---------------------------------------------------------------

# --- Postgres / database ---
DATABASE_USER=
DATABASE_DB=
DATABASE_CLIENT=postgres
DATABASE_HOST=db
DATABASE_PORT=5432
DB_PASSWORD=

# --- pgAdmin UI  http://localhost:8083 ---
DATABASE_EMAIL=
PGADMIN_PASSWORD=

# --- Strapi JWT / tokens ---
APP_KEYS=
JWT_SECRET=
API_TOKEN_SALT=
TRANSFER_TOKEN_SALT=

# --- email app password (create at Google/MS account settings) ---
EMAIL_APP_PASSWORD=

# --- host bound ports ---
DB_PORT=54327
ADMIN_PORT=8083
APP_PORT=9093